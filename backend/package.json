{
  "name": "acupuntura-backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "npx prisma migrate deploy && node index.js",
    "postinstall": "npx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.6.0",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "prisma": "^6.6.0"
  }
}
